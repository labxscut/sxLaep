# Vignettes

Exploratory and packaging notebooks for sxLaep:

| Notebook | Description |
|----------|-------------|
| `RAEP_package.ipynb` | Package / training workflow notes |
| `Marine_Enzyme_Analysis_Pipeline_EN.ipynb` | Marine enzyme analysis pipeline (EN) |

Run Jupyter from the repository root (or any parent of `vignettes/`) so relative paths resolve as expected.
